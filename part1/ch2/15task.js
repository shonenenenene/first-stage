// 2
function checkAge(age) {
   age < 18 ? true : confirm('Родители разрешили?')
}
  
// function checkAge(age) {
//     return (age > 18) || confirm('Родители разрешили?');
//   }

// 3

function min( a, b ) {
    return Math.min(a, b)
}

console.log( min( 76, -100 ) )

// 4

function pow( x, n ) {
    return Math.pow(x, n)
}

